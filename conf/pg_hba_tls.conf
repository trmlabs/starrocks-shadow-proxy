# TLS-required pg_hba for the local TLS test stack. Mirrors AlloyDB's posture.
# TYPE        DATABASE  USER  ADDRESS         METHOD
local         all       all                   trust
hostssl       all       all   0.0.0.0/0       scram-sha-256
hostssl       all       all   ::/0            scram-sha-256
